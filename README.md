# sub-tracker-web
Web frontend for Subtracker built with Next.js and TypeScript. Subscription management dashboard, bank feed linking, and spend analytics. Styled with Tailwind CSS.

## Getting started

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

- `pnpm build` — production build
- `pnpm lint` — run ESLint
